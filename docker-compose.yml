services:
  deskswap:
    build: .
    container_name: deskswap
    ports:
      - "${CONTAINER_PORT:-8080}:8080"
    volumes:
      - ${HOST_PATH:-/home/user}:/home/user
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
